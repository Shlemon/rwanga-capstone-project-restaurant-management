/* 
    
*/
//import { initializeApp } from "firebase/app";
//import { getAnalytics } from "firebase/analytics";

// FS doesn't work with ReactJs because IDK
// Trying different approach **Firestore**
const FIRESTORE_DATA = process.env;
console.log(FIRESTORE_DATA);

export function FetchFromStore(query, item)
{
    return;
}

export function UpdateStore(received)
{
    return;

}